import React from 'react'

const NoMatch = React.createClass({
  displayName: 'NoMatch',
  render () {
    return (
      <div> We're sorry but we were not able to find the page you asked for, let's go back to the homepage ! </div>
    )
  }
})

export default NoMatch
